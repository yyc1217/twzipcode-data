import test from './integrity.test'

const locale = 'en'
const TEST_COUNTY = 'Taipei City'
const TEST_CITY = 'Zhongzheng District'
const TEST_ZIPCODE = '100'

test({
  locale,
  TEST_COUNTY,
  TEST_CITY,
  TEST_ZIPCODE
})
